#!/usr/bin/env bash

# Generated by 🤖 Claude Fable 5
# v0.1

# lock — waylock wrapper: pause break_timer while locked,
# fresh break cycle on unlock.
pkill --signal RTMIN   -f 'bin/break_timer'
waylock -init-color 0xfd7600 -ignore-empty-password   # blocks until unlock
pkill --signal RTMIN+1 -f 'bin/break_timer'
pkill --signal USR2    -f 'bin/break_timer'
