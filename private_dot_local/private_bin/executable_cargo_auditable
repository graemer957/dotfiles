#!/usr/bin/env bash

# Generated by 🤖 Claude Opus 5
# v0.1

# cargo_auditable — cargo-update `--install-cargo` target: it runs
# `<exe> install …`, but cargo-auditable only answers to `cargo auditable …`.
exec cargo auditable "$@"
